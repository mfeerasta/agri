import { ApproverLoginForm } from './approver-login-form';

export default function ApproverLoginPage() {
  return (
    <main className="grid min-h-screen place-items-center p-6 bg-[var(--paper)]">
      <ApproverLoginForm />
    </main>
  );
}
